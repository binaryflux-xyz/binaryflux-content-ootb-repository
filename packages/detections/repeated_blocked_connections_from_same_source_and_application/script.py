# Auto-generated by BinaryFlux UI Builder
# New Detection (new_detection) v1.0.0


def init(event):
    """
    Use appropriate ueba method
    """
    """
    Use appropriate ueba method
    """
    """
    Use appropriate ueba method
    """
    """
    Use appropriate ueba method
    """
    """
    Store the session
    """
    return "initialization completed"

def window():
    return None

def groupby():
    return ["source_ip","source_device_name"]

def investigate():
    return None

def automate():

    return True

def notify():
    return False

def algorithm(event):


    src_ip = event.get('source_ip')
    url = event.get('url')
    key = application.get("blocked_events")

    if key is True:
        return 0.0

    if not src_ip:
        return 0.0
    cidrranges=[] #define cidr ranges
    incidrrange = cidr.inRange(src_ip,cidrranges)

    if incidrrange:
        return 0.0

    log_subtype = event.get('log_subtype')

    if log_subtype == 'webfilter':
        if not url:
            return 0.0

        allowed_url = tpi.query("Whitelistedurls", "url = ?", [url])
        rows = allowed_url.get('rows') if allowed_url else []

        if rows:
            return 0.0

        allowed_url = [row[0] for row in rows]

        if (
            event.get('event_action') == 'blocked'
            and event.get('log_type') == 'utm'
            and url not in allowed_url
        ):
            # application.put("blocked_events", True, 86400)
            return 0.75

    if log_subtype == 'ssl':
        if event.get('event_action') == 'blocked' and event.get('log_type') == 'utm':
            application.put("blocked_events", True, 86400)
            return 0.75

    return 0.0


def clusters(event):



    """
    Store the session
    """
    return "initialization completed"

def context(event):
    """
    Provides a human-readable context message explaining why the detection was triggered.
    Uses event attributes to build this message.
    """
    """
    Provides a human-readable context message explaining why the detection was triggered.
    Uses event attributes to build this message.
    """


    # Safely extract values with defaults
    device = event.get('source_device_name')
    source_ip = event.get('source_ip')
    destination_ip = event.get('destination_ip')
    destination_port = event.get('destination_port')
    policy_id = event.get('policy_id')
    protocol = event.get('network_protocol')
    action = event.get('event_action')
    details = event.get('event_details')
    log_type = event.get('log_type')
    log_subtype = event.get('log_subtype')

    message = (
        "The FortiGate device {device} detected '{action}' event for {log_type} actions with subtype {log_subtype} "
        "for source IP {source_ip}."
        "The most recent event shows a connection to {destination_ip}:{destination_port} "
        "because the firewall {details} using the {protocol} protocol. "
        "Such consistent blocking of outbound connections from a single user or host may indicate automated connection retries, "
        "credential-stuffing activity, or a misconfigured application repeatedly attempting disallowed traffic."
    ).format(
        device=device,
        action=action,
        log_type=log_type,
        source_ip=source_ip,
        protocol=protocol,
        destination_ip=destination_ip,
        destination_port=destination_port,
        details=details,
        log_subtype=log_subtype,
    )
    return message

def criticality():
    return "HIGH"

def tactic():
    return "Command and Control (TA0011)"

def technique():
    return "Application Layer Protocol (T1071)"

def artifacts():
    return stats.collect(["event_action","log_type","log_subtype","source_ip","destination_ip","destination_country","source_device_name","url"])

def correlationArtifacts():
    return ["source_ip","source_device_name"]

def entity(event):
    return {
        "derived": False,
        "value": event.get("source_ip", "unknown"),
        "type": "email"
    }
