title = High Volume of SELECT Queries on Sensitive Tables
description = Detects potential data exfiltration by identifying unusually high volumes of SELECT queries on sensitive database tables by a single account.

      