title=High Connection Load
description=Detects excessive active connections on a virtual IP (VIP) or load-balanced service when total concurrent connections exceed the configured threshold. This may indicate traffic surges, resource exhaustion, application saturation, or denial-of-service conditions impacting service availability.