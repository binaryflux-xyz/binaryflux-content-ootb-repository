title=High HTTP 5xx Error Rate
description=Detects elevated server-side HTTP 5xx errors when responses exceed the configured threshold. This typically indicates backend application failures, upstream dependency issues, crashes, overload conditions, or service instability affecting user requests.